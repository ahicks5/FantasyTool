The Booth web app (Next.js). Run: `npm install && npm run dev` then open http://localhost:3000 (mock data by default; set `NEXT_PUBLIC_API_URL=http://localhost:8000` to hit the FastAPI backend).
Checks: `npm run build`, `npm run lint`, `npm test`.
