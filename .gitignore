.cache/
.env
.env.*
# ...but the template of what to set is the point, so it ships.
!.env.example
__pycache__/
*.pyc
.pytest_cache/
.venv/
node_modules/
web/.next/

# Card render intermediates (the CLI writes the HTML beside each PNG for debugging).
launch/cards/*.html
